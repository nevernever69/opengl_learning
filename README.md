## Dependencies
``` sudo apt install cmake```
``` sudo pacman -S cmake ```
## BUILD
``` make ```
## RUN
  ``` IT will create a binary in the folder you build on```
  just run ```./generated_filename```
